/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['images.unsplash.com','torch.id','down-id.img.susercontent.com', 'shopee.co.id'],
  },
}

module.exports = nextConfig